/**
 * Scratch file left by lane BE3; this sandbox denies file deletion.
 * Kept as a valid no-op so the suite stays green. Safe to `git rm` locally.
 */
import { describe, it, expect } from 'vitest'
describe('probe placeholder', () => {
  it('is a no-op pending deletion', () => { expect(true).toBe(true) })
})
